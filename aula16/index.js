const alunos = ['luiz', 'maria', 'joão'];
//console.log(alunos.length);
alunos.unshift('carla'); //adiiona no inicio da arrays
alunos.push('rauje'); //adiona um elemnto ao final da arrays
alunos.pop('rauje'); //retira um elemento 

console.log(alunos);